name: My first Github Workflow
on: [push]
jobs:
    Test-github-actions:
        runs-on: ubuntu-latest
        steps:
            - run: docker --version
            - run: python --version
            - run: pylint /cw_praca_z_kodem/app.py
            - run: docker build --tag test-app .
            - uses: actions/checkout@v3            
