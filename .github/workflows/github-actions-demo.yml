name: My first Github Workflow
on: [push]
jobs:
    Test-github-actions:
        runs-on: ubuntu-latest
        steps:
            - run: docker --version
            - run: python --version
            - run: python3 -m pylint app.py
            - run: docker build --tag test-app .
            - uses: actions/checkout@v3            
